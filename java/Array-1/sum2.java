public int sum2(int[] nums) {
  int sum=0;
  if (nums.length==0)
  sum=0;
  else if (nums.length<2)
  sum=nums[0];
  else if (nums.length>=2)
  sum=nums[0]+nums[1];
  else
  return sum;
  return sum;
}
